defmodule CommercePlatform.Schema.Query.ProductTest do
  # use CommercePlatform.ConnCase, async: true

  # setup do
  #   CommercePlatform.Seeds.run()
  # end

  # @query """
  # {
  #     products {
  #       name
  #     }
  # }
  # """

  # test "products field returns products" do
  #   conn = build_conn()
  #   conn = get conn, "/api/v1/graphql", query: @query

  #   assert json_response(conn, 200) == %{
  #            "data" => %{
  #              "products" => [
  #                %{"name" => "Meat"},
  #                %{"name" => "Bread"},
  #                %{"name" => "Pizza"}
  #              ]
  #            }
  #          }
  # end
end
